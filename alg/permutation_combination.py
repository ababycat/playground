def permutation(index_list, num):
    choice = []
    
    index_choice = index
    
    if num == 1:
        for val in index_list:
            choice += [val]
        return choice
    
    for i in range()
        choice += permutation(index_list, num)
    num = num - 1
    
    return choice


permutation([1, 2, 3], 1)